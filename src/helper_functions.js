function canGoOut(city, state, activity, number_of_people, time) {
  return Math.random() > 0.5;
}

function getActivityList(place_type){
  return ["Freizeiteinrichtungen", "Schulen", "Lieferdienste"]
}
